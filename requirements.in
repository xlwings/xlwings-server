# List your own top-level dependencies in this file. After editing, run:
# python run.py deps compile
# This generates a series of requirements.txt files with all dependencies incl.
# sub-dependencies pinned (a 'lock' file).
#
# If you want to upgrade all packages to the latest version, run:
# python run.py deps upgrade

-r requirements-core.txt  # Don't delete this line
azure-functions; sys_platform != 'win32'
numpy==2.0.1  # 2.1.0 doesn't offer a build for Python 3.9
pandas
