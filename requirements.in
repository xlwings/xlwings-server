# These are the top level dependencies
# They could be used with a tool such as pip-tools or just manually:
# pip install requirements.in
# pip freeze > requirements.txt
cachetools
fastapi[all]
gunicorn
jinja2
jinja2-fragments
pandas
PyJWT[crypto]
python-socketio
uvicorn[standard]
xlwings
